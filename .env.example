# Hermes API Configuration
# Copy this file to .env and customize as needed

# Server Configuration
# Host address to bind the server (0.0.0.0 for all interfaces)
HOST=0.0.0.0

# Port number for the API server
PORT=8080

# Logging Configuration
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Python Configuration
# Enable unbuffered Python output for real-time logging
PYTHONUNBUFFERED=1

# Model Configuration (Optional)
# Override default model paths if needed
# WHISPER_MODEL_PATH=/path/to/whisper/models
# TTS_MODEL_PATH=/path/to/tts/models
# SPACY_MODEL_PATH=/path/to/spacy/models
# SENTENCE_TRANSFORMER_MODEL_PATH=/path/to/sentence-transformer/models

# Performance Tuning (Optional)
# Number of uvicorn workers (default: 1)
# WORKERS=1

# Maximum request size in bytes (default: 100MB for audio files)
# MAX_REQUEST_SIZE=104857600

# Sophia Integration (required for /ingest/media endpoint)
# Host address for Sophia cognitive service
SOPHIA_HOST=localhost

# Port for Sophia service (default 8000 for local dev, 8001 for Docker)
SOPHIA_PORT=8000

# API token for authenticating with Sophia (required for media ingestion)
# Must match SOPHIA_API_TOKEN set on the Sophia process
SOPHIA_API_KEY=sophia_dev

# Integration URLs (for documentation purposes)
# These are examples of how other LOGOS components would be configured
# SOPHIA_URL=http://sophia:8000
# APOLLO_URL=http://apollo:3000
# TALOS_URL=http://talos:8081

# Milvus Configuration
# Host address for Milvus vector database
MILVUS_HOST=localhost

# Port for Milvus vector database
MILVUS_PORT=18530

# Collection name for storing embeddings
MILVUS_COLLECTION_NAME=hermes_embeddings

# Neo4j Configuration
# URI for Neo4j graph database
NEO4J_URI=bolt://localhost:18687

# Neo4j username
NEO4J_USER=neo4j

# Neo4j password
NEO4J_PASSWORD=neo4jtest

# Repository Root Configuration
# Override automatic detection of repo root (used by tests and scripts)
# HERMES_REPO_ROOT=/path/to/hermes
